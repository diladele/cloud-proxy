apiVersion: v1
kind: Service
metadata:
  name: websafety-ui
spec:
  type: NodePort
  selector:
    app: websafety-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: ui
      nodePort: 30080
